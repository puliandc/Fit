# FIT 应用外部训练计划文件系统实现方案

//created by Jason Lu on 10:30:00 10/12/2025

## 项目概述

本文档描述了为 FIT 应用添加外部 JSON 训练计划文件功能的简化实现方案。用户将能够通过简单的文件选择功能来导入训练计划，实现个人健身训练的灵活管理。

## 1. 简化的架构设计

### 1.1 整体架构

保持简单的三层架构：

- **界面层**：负责用户界面展示和文件选择功能
- **业务层**：处理训练计划的基础数据验证
- **数据访问层**：统一管理外部文件读取和数据解析

### 1.2 数据流程

用户选择文件 → 界面层 → 业务层 → 数据访问层 → 文件系统

**核心功能：**
- 从 iPhone 下载文件夹选择 JSON 文件
- 验证文件格式是否符合要求
- 解析文件内容并显示训练计划

## 2. 简化的数据模型

### 2.1 核心数据结构

只保留最基本的训练数据结构，移除所有冗余字段：

```json
{
  "训练计划名称": "胸部训练计划",
  "练习项目": [
    {
      "练习名称": "卧推",
      "组数设置": [
        {
          "组数类型": "正式组",
          "目标次数": 12,
          "目标重量": 60,
          "休息时间": 90
        }
      ]
    }
  ]
}
```

### 2.2 数据字段说明

**训练计划字段：**
- `训练计划名称`：训练计划的名称，如"胸部训练计划"

**练习项目字段：**
- `练习名称`：具体练习动作的名称，如"卧推"

**组数设置字段：**
- `组数类型`：组数类型，如"正式组"、"热身组"
- `目标次数`：该组需要完成的次数
- `目标重量`：该组的目标重量（公斤）
- `休息时间`：组间休息时间（秒）

## 3. 文件管理系统

### 3.1 文件选择机制

**文件来源：**
- 直接从 iPhone 的下载文件夹选择 JSON 文件
- 支持通过 AirDrop 传输文件到下载文件夹
- 用户可以自由选择任何符合格式的 JSON 文件

**操作流程：**
1. 用户点击 Main 界面的"读取计划"按钮
2. 自动打开 iPhone 下载文件夹的文件选择器
3. 用户选择 JSON 文件
4. 系统验证文件格式并解析内容
5. 显示训练计划内容

### 3.2 文件格式验证

**基础验证要求：**
- 文件必须是有效的 JSON 格式
- 必须包含"训练计划名称"字段
- 必须包含"练习项目"数组
- 每个练习项目必须包含"练习名称"和"组数设置"
- 每个组数设置必须包含完整的组数信息

**错误处理：**
- 文件格式错误时，弹出提示窗口告知用户"文件格式错误，请检查JSON文件格式"
- 文件内容不符合要求时，提示用户"文件内容格式不正确，请按照标准格式编写"

## 4. 用户界面改进

### 4.1 Main 界面修改

**按钮功能更改：**
- 将现有的"读取计划"按钮改为文件选择功能
- 点击后直接打开 iPhone 下载文件夹的文件选择器
- 按钮文字可以改为"选择训练计划文件"

**界面布局保持：**
- 保持现有的界面布局不变
- 只修改按钮的功能，不增加新的界面元素

### 4.2 错误提示界面

**简化的错误处理：**
- 文件读取失败时，显示简单的弹窗提示
- 提示内容包括错误类型和解决建议
- 用户点击"确定"后返回主界面

## 5. 数据处理逻辑

### 5.1 文件读取流程

**读取步骤：**
1. 用户选择文件后，检查文件扩展名是否为 `.json`
2. 验证 JSON 格式的有效性
3. 检查必要字段是否存在
4. 验证数据类型是否正确
5. 解析文件内容并更新界面

**数据处理：**
- 每次只读取一个文件，不需要驻留内存
- 文件读取完成后直接显示训练计划
- 不需要缓存机制，每次重新选择文件时重新读取

### 5.2 数据验证规则

**必填字段验证：**
- `训练计划名称`：字符串类型，不能为空
- `练习项目`：数组类型，至少包含一个练习
- `练习名称`：字符串类型，不能为空
- `组数设置`：数组类型，至少包含一个组数设置

**数据类型验证：**
- `目标次数`：正整数
- `目标重量`：正数
- `休息时间`：正整数

## 6. 测试要求

### 6.1 基本功能测试

**测试范围：**
- 文件选择功能正常工作
- JSON 文件格式验证正确
- 错误提示功能正常显示
- 训练计划内容正确解析和显示

### 6.2 兼容性测试

**测试环境：**
- 设备：iPhone 12
- 系统：iOS 26
- 不需要考虑其他设备和系统版本的兼容性

### 6.3 成功标准

**验收标准：**
- 用户能够通过文件选择功能导入训练计划
- 文件格式验证功能正常工作
- 错误提示清晰明了
- 训练计划能够正确显示和执行

**测试通过即项目完成：**
- 无需性能测试和安全测试
- 无需复杂的测试用例和自动化测试
- 您手动测试通过即为项目成功

## 7. 实施重点

### 7.1 核心开发任务

**主要工作内容：**
1. 修改 Main 界面的"读取计划"按钮功能
2. 实现文件选择器功能
3. 添加 JSON 文件格式验证
4. 实现简单的错误提示机制
5. 测试基本功能是否正常工作

### 7.2 简化开发流程

**开发原则：**
- 专注于核心功能，不添加额外特性
- 保持代码简单易懂，便于维护
- 不考虑性能优化和高级功能
- 以满足个人使用需求为目标

## 8. 注意事项

### 8.1 使用说明

**用户操作指南：**
1. 准备符合格式的 JSON 训练计划文件
2. 通过 AirDrop 或其他方式将文件传输到 iPhone 下载文件夹
3. 打开 FIT 应用，点击"选择训练计划文件"按钮
4. 从下载文件夹中选择对应的 JSON 文件
5. 系统自动解析并显示训练计划内容

### 8.2 文件格式示例

**标准 JSON 格式示例：**
```json
{
  "训练计划名称": "胸部训练计划",
  "练习项目": [
    {
      "练习名称": "卧推",
      "组数设置": [
        {
          "组数类型": "正式组",
          "目标次数": 12,
          "目标重量": 60,
          "休息时间": 90
        },
        {
          "组数类型": "正式组",
          "目标次数": 10,
          "目标重量": 65,
          "休息时间": 90
        }
      ]
    },
    {
      "练习名称": "哑铃飞鸟",
      "组数设置": [
        {
          "组数类型": "正式组",
          "目标次数": 15,
          "目标重量": 20,
          "休息时间": 60
        }
      ]
    }
  ]
}
```

---

**总结：**
本方案专注于为 FIT 应用添加简单、实用的外部文件导入功能，通过简化的架构设计和核心功能实现，满足个人健身训练的灵活管理需求。