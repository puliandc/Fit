# Fit 项目业务词典

//created by Jason Lu on 09:17:00 10/12/2025

## 📖 业务名词定义

### 核心概念

**训练 (Workout)**
- 一次完整的训练活动
- 包含多个训练组数
- 有明确的开始和结束时间
- 记录训练总时长

**训练组 (WorkoutSet)**
- 训练的基本单位
- 包含重量和次数两个属性
- 记录完成的时间戳
- 不可修改，只可删除

**训练项目 (Exercise)**
- 具体的训练动作
- 例如：深蹲、卧推、硬拉
- 在MVP阶段简化处理

### 数据模型定义

```swift
// 训练记录
struct Workout {
    id: UUID           // 唯一标识符
    date: Date         // 训练日期
    sets: [WorkoutSet] // 训练组数列表
    duration: TimeInterval // 训练时长
}

// 训练组数
struct WorkoutSet {
    id: UUID      // 唯一标识符
    weight: Double // 重量（公斤）
    reps: Int     // 次数
    timestamp: Date // 完成时间
}
```

## 🎯 业务规则定义

### 训练规则

**规则1：训练组数有效性**
- 重量必须 >= 0
- 次数必须 >= 1
- 小数点精度：重量保留1位小数
- 整数验证：次数必须是正整数

**规则2：训练完整性**
- 一次训练必须至少包含1个有效组数
- 训练时长必须 >= 1分钟
- 训练日期不能是未来时间

**规则3：数据不可变性**
- 已保存的训练组数不可修改
- 只能删除错误的组数记录
- 训练记录一旦完成不可修改

### 用户交互规则

**规则4：输入验证**
- 所有输入字段必须有验证
- 实时验证用户输入
- 提供清晰的错误提示

**规则5：确认机制**
- 删除操作需要用户确认
- 完成训练需要确认
- 重大操作有撤销选项

## 🔄 业务流程

### 训练流程

```
1. 用户点击"开始训练"
   ↓
2. 进入训练界面
   ↓
3. 添加训练组数
   ↓
4. 继续添加或完成训练
   ↓
5. 训练完成，显示统计
   ↓
6. 保存训练记录
```

### 编辑流程

```
1. 用户点击添加组数
   ↓
2. 弹出编辑对话框
   ↓
3. 输入重量和次数
   ↓
4. 实时验证输入
   ↓
5. 确认保存或取消
```

## 🎭 状态机定义

### 训练状态

```swift
enum WorkoutState {
    case notStarted    // 未开始
    case inProgress    // 进行中
    case editingSet    // 编辑组数
    case completed     // 已完成
}
```

### 应用状态

```swift
enum AppState {
    case loading       // 加载中
    case main          // 主界面
    case workout       // 训练界面
    case editSet       // 编辑界面
}
```

## 📊 数据约束

### 数值范围

**重量约束**：
- 最小值：0 kg
- 最大值：1000 kg
- 精度：0.1 kg

**次数约束**：
- 最小值：1 次
- 最大值：1000 次
- 类型：正整数

**时间约束**：
- 最短训练：1 分钟
- 最长训练：8 小时
- 时间精度：秒

### 字符串约束

**文本长度**：
- 训练备注：最多200字符
- 输入框：最多10字符（数字输入）
- 按钮文字：最多20字符

## 🔐 权限规则

### 数据访问权限

**读取权限**：
- 用户只能查看自己的训练数据
- 数据按时间倒序显示
- 支持分页加载

**写入权限**：
- 用户只能创建和删除自己的数据
- 批量操作需要确认
- 数据变更有日志记录

## 🚫 业务限制

### 功能限制

**MVP阶段限制**：
- 不支持训练计划管理
- 不支持历史数据分析
- 不支持数据导入导出
- 不支持社交功能

**数据限制**：
- 单次训练最多100组
- 本地存储最多1000次训练
- 不支持云端同步

### 使用限制

**频率限制**：
- 每分钟最多添加50组
- 每天最多创建10次训练
- 防止恶意数据操作

## 🎨 用户体验规则

### 界面规则

**响应时间**：
- 页面切换 < 300ms
- 数据加载 < 1s
- 保存操作 < 500ms

**反馈机制**：
- 所有操作都有视觉反馈
- 错误状态明确提示
- 加载状态有指示器

### 可访问性规则

**VoiceOver支持**：
- 所有界面元素有标签
- 操作有语音提示
- 导航逻辑清晰

**字体支持**：
- 支持动态字体大小
- 保持界面布局合理
- 文本对比度符合标准

## 🔄 版本演进

### v1.0 (MVP)
- 基础训练记录功能
- 简单的组数管理
- 本地数据存储

### v1.1 (计划)
- 训练统计功能
- 数据导出功能
- UI优化

### v2.0 (计划)
- 训练计划管理
- 数据同步功能
- 社交功能

---

这份业务词典为Fit项目提供了统一的业务语言和规则定义，确保所有开发者和AI助手对业务概念有一致的理解。