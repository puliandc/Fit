# Fit 应用 UI 架构

//created by Jason Lu on 09:17:00 10/12/2025

## 📱 整体界面结构

### 应用层级结构
```
FitApp (App Entry)
└── ContentView (Root View)
    ├── MainScreen (主界面)
    │   ├── 今日训练概览
    │   ├── 快速开始按钮
    │   └── 底部导航
    └── WorkoutScreen (训练界面)
        ├── 训练执行区域
        ├── 组数编辑器
        └── 完成训练流程
```

## 🎨 核心UI模块

### 1. 主界面 (MainScreen)

**功能**：用户进入应用的主要界面，显示今日训练状态

**组件构成**：
- **训练概览卡片** - 显示今日训练计划
- **快速开始按钮** - 一键开始训练
- **历史记录入口** - 查看过往训练
- **进度统计** - 简单的进度可视化

**交互逻辑**：
```swift
NavigationLink(destination: WorkoutScreen()) {
    ActionButton(title: "开始训练", style: .primary)
}
```

### 2. 训练界面 (WorkoutScreen)

**功能**：核心训练功能界面，用于记录训练数据

**组件构成**：
- **当前训练项目显示** - 正在进行的训练动作
- **组数列表** - 显示已完成的组数
- **添加组数按钮** - 记录新的训练组
- **完成训练按钮** - 结束当前训练

**关键交互**：
- 点击"添加组数" → 弹出编辑对话框
- 滑动组数 → 删除或编辑
- 完成所有组数 → 显示完成统计

### 3. 编辑组数对话框 (EditSetDialog)

**功能**：编辑单组训练数据（重量、次数）

**输入组件**：
- **重量输入框** - 数字键盘输入
- **次数输入框** - 数字键盘输入
- **确认/取消按钮** - 保存或放弃更改

**验证规则**：
- 重量 >= 0，小数点后1位
- 次数 >= 1，整数
- 必填字段验证

## 🎯 设计系统组件

### 1. 导航组件 (CompatibleNavigationView)

**用途**：提供统一的应用导航体验

**特性**：
- 兼容不同iOS版本的导航
- 统一的标题样式
- 返回按钮自定义

```swift
CompatibleNavigationView(title: "训练记录") {
    // 内容视图
}
```

### 2. 操作按钮 (ActionButton)

**用途**：统一的按钮样式和行为

**样式变体**：
- `.primary` - 主要操作（开始训练）
- `.secondary` - 次要操作（添加组数）
- `.danger` - 危险操作（删除记录）

```swift
ActionButton(title: "开始训练", style: .primary) {
    // 动作处理
}
```

### 3. 玻璃卡片 (GlassCard)

**用途**：提供现代化的卡片容器效果

**视觉效果**：
- 毛玻璃背景
- 圆角边框
- 阴影效果

```swift
GlassCard {
    VStack {
        // 卡片内容
    }
}
```

## 🎨 视觉设计系统

### 颜色规范

**主色调**：
- 主色：`#007AFF` (系统蓝)
- 背景：`#F2F2F7` (浅灰)
- 卡片：`#FFFFFF` (白色)

**状态色**：
- 成功：`#34C759` (绿色)
- 警告：`#FF9500` (橙色)
- 错误：`#FF3B30` (红色)

### 字体规范

**标题字体**：
- 大标题：32pt，Bold
- 标题：24pt，Semibold
- 副标题：18pt，Medium

**正文字体**：
- 正文：16pt，Regular
- 辅助文字：14pt，Regular
- 说明文字：12pt，Regular

### 间距规范

**基础间距**：8pt的倍数
- 微间距：4pt
- 小间距：8pt
- 中间距：16pt
- 大间距：24pt
- 特大间距：32pt

## 📐 布局原则

### 1. 响应式设计

**适配不同屏幕尺寸**：
- iPhone SE：紧凑布局
- iPhone 标准：标准布局
- iPhone Plus/Max：宽松布局

### 2. 安全区域

**遵循iOS安全区域**：
- 顶部避开状态栏
- 底部避开主屏幕指示器
- 侧边避开圆角区域

### 3. 可访问性

**支持辅助功能**：
- VoiceOver 朗读支持
- 动态字体大小支持
- 高对比度模式适配

## 🔄 状态管理

### 界面状态

**加载状态**：
- 显示加载指示器
- 禁用用户交互
- 提供加载说明文字

**错误状态**：
- 错误图标和说明
- 重试操作按钮
- 返回上级选项

**空状态**：
- 引导性插图
- 操作建议文字
- 主要操作按钮

## 🎭 动画效果

### 页面转场

**导航转场**：
- 标准推送动画
- 模态弹出动画
- 淡入淡出效果

**微交互**：
- 按钮点击反馈
- 卡片悬浮效果
- 列表滑动动画

### 动画时长

**快速交互**：0.2秒
**标准动画**：0.3秒
**复杂转场**：0.5秒

---

这份UI架构文档为Fit应用的前端开发提供了完整的界面设计指南，确保界面的一致性和用户体验的流畅性。